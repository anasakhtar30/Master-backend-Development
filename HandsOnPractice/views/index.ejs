<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body> 
    <div class="main w-full min-h-screen bg-zinc-900 border-2 border-cyan-800">
        <div class="form p-10 text-white border-2 border-cyan-800">
            <form action="/create" method = "post" class="border-2 border-cyan-800">
                <input class = "block w-full rounded-md outline-none px-5 py-2 bg-zinc-800" type="text" placeholder="Title goes here..." name = "title">
                <textarea class = "block w-full rounded-md outline-none px-5 py-2 mt-2 bg-zinc-800 resize-none" placeholder="Write your task detail here" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks  p-10 flex gap-4 flex-wrap border-2 border-cyan-800">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task w-82 px-5 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracker-tighter" ><%=val%></h1>
                        <div class="flex w-full justify-between items-center mt-3">
                            <a class="text-blue-500  inline-block" href="/files/<%= val%>">Read more</a>
                            <a class="text-zinc-600" href="/edit/<%= val%>">Edit Filename</a>
                        </div>
                        
                    </div>
                <% })%>
           <% } else{ %>
                <h3 class = "text-zinc-600">No task yet</h3>
           <% } %>
        </div>
    </div>

    
</body>
</html>